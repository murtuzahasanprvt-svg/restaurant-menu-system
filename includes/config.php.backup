<?php
/**
 * System Configuration
 */

// Database Configuration
define('DB_HOST', 'sql312.infinityfree.com');
define('DB_NAME', 'if0_39497403_qr');
define('DB_USER', 'if0_39497403');
define('DB_PASS', 'your_password_here');
define('DB_CHARSET', 'utf8mb4');

// Application Configuration
define('APP_NAME', 'Restaurant Menu System');
define('APP_URL', 'https://your-domain.com'); // Update with your actual domain
define('APP_VERSION', '1.0.0');

// Paths (only define if not already defined)
if (!defined('ROOT_PATH')) {
    // This should be defined by the main entry point (index.php)
    // If not defined, set it to parent directory (for includes/config.php)
    define('ROOT_PATH', dirname(__DIR__));
}
if (!defined('APP_PATH')) {
    define('APP_PATH', ROOT_PATH . '/app');
}
if (!defined('INCLUDES_PATH')) {
    define('INCLUDES_PATH', ROOT_PATH . '/includes');
}
if (!defined('THEMES_PATH')) {
    define('THEMES_PATH', ROOT_PATH . '/themes');
}
if (!defined('ADDONS_PATH')) {
    define('ADDONS_PATH', ROOT_PATH . '/addons');
}
if (!defined('PUBLIC_PATH')) {
    define('PUBLIC_PATH', ROOT_PATH . '/public');
}
if (!defined('UPLOADS_PATH')) {
    define('UPLOADS_PATH', ROOT_PATH . '/uploads');
}
if (!defined('QR_CODE_PATH')) {
    define('QR_CODE_PATH', ROOT_PATH . '/uploads/qrcodes');
}

// Session Configuration (only if not defined)
if (!defined('SESSION_LIFETIME')) {
    define('SESSION_LIFETIME', 3600); // 1 hour
}
if (!defined('SESSION_NAME')) {
    define('SESSION_NAME', 'restaurant_menu_session');
}

// Security Configuration (only if not defined)
if (!defined('CSRF_TOKEN_NAME')) {
    define('CSRF_TOKEN_NAME', 'csrf_token');
}
if (!defined('HASH_COST')) {
    define('HASH_COST', 12);
}
if (!defined('MAX_LOGIN_ATTEMPTS')) {
    define('MAX_LOGIN_ATTEMPTS', 5);
}
if (!defined('LOCKOUT_DURATION')) {
    define('LOCKOUT_DURATION', 900); // 15 minutes in seconds
}

// QR Code Configuration (only if not defined)
if (!defined('QR_CODE_SIZE')) {
    define('QR_CODE_SIZE', 300);
}
if (!defined('QR_CODE_PREFIX')) {
    define('QR_CODE_PREFIX', 'QR-');
}

// Error Reporting
if (!defined('DEBUG_MODE')) {
    define('DEBUG_MODE', true);
}
if (DEBUG_MODE) {
    error_reporting(E_ALL);
    ini_set('display_errors', 1);
} else {
    error_reporting(0);
    ini_set('display_errors', 0);
}

// Timezone
date_default_timezone_set('UTC');

// Ensure upload directories exist
if (!file_exists(UPLOADS_PATH)) {
    mkdir(UPLOADS_PATH, 0755, true);
}
if (!file_exists(QR_CODE_PATH)) {
    mkdir(QR_CODE_PATH, 0755, true);
}
?>